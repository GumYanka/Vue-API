import { state } from "@/store/modules/cards/state";
import { getters } from "@/store/modules/cards/getters";
import { mutations } from "@/store/modules/cards/mutations";
import { actions } from "@/store/modules/cards/actions";

export const cards = {
  state,
  getters,
  mutations,
  actions
};